# Clancy — venture spec

Jacky's second venture alongside running MegaStar Arena: **Clancy**, an AI agency selling a **CRM / automation platform for local businesses**, built and owned as original code (multi-tenant SaaS), sold as a productized subscription. Clancy is the working name for both company and product (decided 2026-07-07); **entity registration pending — Jacky will confirm when registered**. Brand identity + marketing: see `branding-marketing.md`. Ambition (stated 2026-07-07): this becomes Jacky's **full-time business** — the venture is planned on a 5-year horizon, not as a permanent side project. Spun off from the learnings of the MegaStar Arena CRM (`../megastar-crm`) — reuses its architecture lessons, **not** its code. This folder is the product's home; no code exists yet.

> **This file is the canonical spec.** It replaced a long decision journal in memory (2026-07-04). When decisions change, update this file — don't fork the plan into chat memory.

## Strategic position (decided 2026-07-04)
- **Own-layer-plus-compose-APIs** — build and own the CRM/pipeline/orchestration layer; integrate best-in-class infrastructure APIs instead of building deep infra: Twilio (SMS), an email API (Resend/Postmark) for deliverability, Google Business Profile API (reviews). Rejected: white-labeling GoHighLevel (no ownership), full from-scratch (multi-year). GHL is the *shape* reference, not the codebase.
- **AI receptionist / voice is out of scope entirely** (Jacky's call) — removes telephony/voice AI, the hardest integration, from the critical path.
- **Engine + vertical template packs**, not one-size-fits-all: a generic core engine, with per-vertical templates (fields, pipeline stages, seed config — as data) applied as a **configuration step** — not a separate build phase per vertical. Rejected a single maximally-generic template (commoditized lane vs GHL/HubSpot/Zoho, no differentiation).
- **In-product AI removed entirely (2026-07-07, Jacky's call)** — no Claude API anywhere in the client product: no chat widget, no AI-drafted replies or emails. Follow-up sequences and review requests run as deterministic, templated automations (triggers + merge fields). This kills per-tenant AI usage costs and the AI-liability risk; differentiation now rests on the **personally-tailored build + Managed service**, not AI features. Claude Code remains Jacky's dev tool for building and maintaining the platform. *(Supersedes the 2026-07-04 "Claude API embedded in the backend" decision and the AI-layer guardrail rules that came with it — if AI is ever reintroduced, revisit those bitter-lesson guardrails: tool-loop agents not scripted flows, determinism at money/sending boundaries, prompts as versioned config, one model gateway, evals from day one.)*

## Feature scope (7 features, AI-free)
Professional website with live reviews · review automation · online booking · follow-up email sequences · 6–9 stage lead pipeline · unified inbox · CRM & contacts.

## Build plan v2 (current roadmap)
1. **Core engine + CRM** — multi-tenant org-scoped base, contacts + CSV import, configurable 6–9 stage lead pipeline, auth/roles
2. **Booking + follow-ups** — online booking calendar, follow-up sequence builder wired to pipeline stage changes, email API (Resend/Postmark) integration
3. **Website + reviews** — templated (not drag-and-drop) website generator per vertical; Google Business Profile API for automated review requests + monitoring; website pulls live reviews from the same integration
4. **Unified inbox** — all customer messages in one place for staff to answer; website contact form messages (auto-creating CRM contacts) + SMS (Twilio) first; Instagram/Facebook/WhatsApp added later, **demand-gated** — hardest integrations sequenced last
5. **Expand + launch** — remaining inbox channels, vertical template packs, self-serve signup, pricing, public launch whenever ready (**no fixed date**)

## Architecture rules
- **True multi-tenant from day one:** one shared codebase/DB, `organization_id` scoping + RLS on **every** table, one org = one client.
- Everything per-tenant configurable (departments, pipeline stages, fields) as data — **no hardcoded enums enforced by DB CHECK constraints** (the recurring MSA CRM production gotcha; see `../megastar-crm/CLAUDE.md`).
- Colour system (confirmed): **purple** = brand/primary · **gray/amber/purple/green** = pipeline status. *(Teal was reserved for AI actions — freed up since in-product AI was removed 2026-07-07.)*
- Automations (follow-up sequences, review requests) are **deterministic**: trigger + template + merge fields. Hard rate limits on all outbound sending regardless.

## Infrastructure & data ownership (decided 2026-09-05)
Three questions resolved once Jacky bought an M4 Mac mini + a UGREEN DXP4800 Plus NAS (single 10TB drive) and asked whether to self-host, switch database providers, or give each client their own accounts:
- **Database stays on Supabase — NOT Neon.** The whole security model depends on Supabase-specific pieces: RLS helpers (`auth_org_id`, `auth_role`, `is_platform_admin`), the `handle_new_user` trigger on `auth.users`, and three storage buckets. Neon is Postgres-only — moving there means rebuilding auth + file storage and rewriting every RLS policy (the one part currently database-enforced and solid). Not worth dodging a ~USD 25/mo bill. When scale demands it, the upgrade is **Supabase Pro** (daily backups + PITR), triggered the day the first client pays.
- **The NAS is a backup target — NOT a production database.** Capable hardware (Intel N100, Docker), wrong place: the app runs on Vercel, so a home-hosted DB routes every client request through Jacky's residential internet (likely CGNAT, no public IP), and puts every client site behind his home power/network. One 10TB drive in a 4-bay enclosure is **zero redundancy** — a step *down* from Supabase on durability. Also cuts against the 2026-07-04 "compose infra APIs, don't build deep infra" call. Correct use: scheduled `pg_dump` from Supabase + the three storage buckets, landed on the NAS via the Mac mini (idles ~5W). Mirror the drive (3 bays free); add a cheap offsite copy as the third leg (NAS + Mac mini share one building = one fire/flood/theft). DB is text, <1GB for years.
- **Infrastructure stays Clancy-owned (shared multi-tenant) — NOT per-client Vercel/Supabase accounts.** Client-owned accounts would undo the first architecture rule (one app instance = one database): each client would need their own deployment, i.e. the bespoke-fork-per-client model explicitly banned. At 20 clients, one security patch becomes 20 deploys; the template strategy has nothing to template *into*; every renewal becomes a migration project (contradicts the Managed→Standard "tier flip, not handover" design). It also doesn't even reduce support load — Managed work still needs the credentials, now spread across 20 accounts Jacky doesn't control. **The ownership clients actually care about is delivered by registering each client's domain in their name, pointed at Clancy infra** — costs nothing, changes no architecture, and paired with an export feature gives genuine, honest portability. Near-zero marginal cost per client is Clancy's biggest structural advantage; don't trade it away to save ~RM 210/mo.

## Verticals (template packs, in order)
1. **Car workshops** — job pipeline (intake → diagnosis → in progress → completed/invoiced); closest to the proven MSA pipeline shape
2. **Dine-in F&B** — POS/table/menu-driven, structurally different core flows
3. **Handmade/D2C social-commerce brand** — syncs orders from existing e-commerce (e.g. Shopify) rather than replacing it; batch-production pipeline (New → Batching → Packed → Shipped); a second B2B collab pipeline (Inquiry → Discussing → Active → Delivered); social inbox (IG/TikTok DMs) as primary inbox channel; repeat-purchase/gifting nurture; stock-waitlist automation. *(Defined via the BYYOU KL design exercise — not a real pilot lead.)*

## Business model
- Productized SaaS subscription (monthly per client org, on the shared multi-tenant platform). Price tiers **not set** — validate via early customer conversations.
- **Sales motion** (decided 2026-07-07):
  1. Demo the live MegaStar Arena CRM as proof-of-concept
  2. Propose a version customized to the prospect's own workflow (Jacky prompts Claude to configure/build it — client never touches code)
  3. Give the prospect a live test-run/trial on their own site before they commit
  4. Quote: **$0 setup fee**, monthly subscription, **1-year lock-in**
  5. During the locked-in year, Jacky personally handles all maintenance/updates — this is the **Managed** tier
  6. At renewal: client either signs another Managed year, or drops to **self-serve** (Standard) — same shared platform, same account and data, Jacky just stops actively managing it and the client owns future changes. This is a **tier/plan flip, not an infrastructure handover** — no per-client export or standalone-instance work required.
- This operationalizes the previously-flagged **Standard vs Managed** tiering: every client starts on Managed for year 1 (bundled into the lock-in); Standard/self-serve becomes available only as the no-renewal default after year 1, not a signup-time choice. With in-product AI removed, Managed-tier work is genuinely Jacky's hours — which makes the Managed cohort cap below a hard rule, not a guideline.
- **Pricing (confirmed 2026-07-07, premium positioning):** Managed **RM 1,200/mo**, Self-Serve **RM 500/mo**, setup free. Pilot variant for first 1–2 clients: 50% off year one in exchange for feedback + testimonial.
- **Managed capacity is a rule, not a permanent cap (reframed 2026-07-07):** the earlier "3–5 clients max" was Jacky's part-time hours, not the product's ceiling — the multi-tenant architecture holds unlimited orgs. Rule: Managed slots = what current delivery capacity can genuinely serve well (~3–5 per part-time solo founder; ~15–20 per full-time account manager later). Capacity grows with the team; scarcity marketing must always reflect the true current number.
- **Sales assets** (created 2026-07-07): `client-intake-checklist.md` (everything to collect from a client before building) and `sales-proposal-template.md` (pitch → trial → close playbook, incl. objection handling and pilot-client variant).
- **Planning & brand docs** (2026-07-07): `five-year-plan.md` (year-by-year targets, full-time gate, foundations, risks) · `branding-marketing.md` (Direction C identity + content system) · `launch-posts.md` (the 9 launch posts, copy + pre-launch checklist).

## Constraints & working rules
- **Time budget today: ~1–2 days/week** (Jacky runs MegaStar Arena full-time as Director). Scope every batch to that reality. The stated ambition is to go **full-time on Clancy** once a revenue gate is hit (gate to be defined in the 5-year plan) — until then, plan for part-time capacity and don't let ambition inflate near-term scope.
- **Product, not projects:** every client-specific request must land as reusable configuration or a platform feature — never a bespoke code fork for one client. This is the rule that makes year-4 scale possible and it applies from client #1.
- **No fixed launch date.** Suggested informal checkpoints (core engine usable → workshop template live with a real pilot → F&B template) — Jacky hasn't committed to them yet.
- Draft-first workflow applies here too: plan in chat, wait for confirmation, then execute the confirmed scope in full.
- Keep it simple first; one vertical fully validated beats two half-built.

## Governance flags (before charging real external clients — not urgent at pilot stage)
- Separate legal entity from MegaStar Arena (IP ownership, liability ring-fencing) — **in motion: Jacky plans to register Clancy; unconfirmed until he says so.** Contracts should name Clancy (the company), not Jacky personally.
- Infra separation status (2026-07-07): Supabase = own Clancy org ✓ · GitHub = jackywong29/clancy ✓ · Vercel = deployed under Jacky's personal hobby account (named "MSA") — acceptable for pilot; transfer the project to a Clancy team/account once the entity exists.
- Contracts need the 1-year lock-in and Managed→Standard downgrade spelled out explicitly (what happens on early cancellation, what "self-serve" means for support/updates going forward). *(The AI-liability clause flagged 2026-07-04 is no longer needed — in-product AI removed 2026-07-07; standard SaaS terms suffice.)*
- Malaysia **PDPA** applies once storing clients' customers' contact data

## Client CRM (Batch 5, 2026-07-10) — the client's OWN internal management system
Each client business needs TWO things from Clancy: (1) their **website** (`sites` config, `/s/<slug>`) and (2) their **CRM** — a configurable internal tracker, the back-end analogue of the website. Both customized the same way (config-driven editors).
- **Model:** `organizations.crm_config` JSONB (record label singular/plural, custom `fields`, `card_fields`, `modules`) drives what a workspace tracks. Record values live in `clients.custom` JSONB — adding a field never needs a migration. Migration `011_client_crm.sql`.
- **Routing branches by workspace:** Clancy's own workspace keeps its **dedicated sales board** (`/pipeline` + `/clients/*`, untouched — "perfect for Jacky's end"). **Client** workspaces (slug ≠ clancy) get the configurable **records CRM**: `/pipeline` renders `RecordsBoard`, records at `/records/new` + `/records/[id]`, config editor at `/crm`. Header nav swaps accordingly.
- **Who edits:** workspace members (clients) can self-edit their own CRM config (RLS `org members update org`); Jacky as platform admin edits any client's CRM/site by switching workspace (header switcher). Confirmed direction 2026-07-10: clients self-edit allowed + admin full access.
- **First seed:** SGCKL = a "Visitors" retention CRM (fields: first_visit, how_heard, cell_group, invited_by).
- **Batch 6 shipped (2026-07-10): tasks + calendar modules.** `tasks` table (status pending/in_progress/done as text — validated in app, no CHECK enums; assignee, due date, optional record link) at `/tasks`; `events` table + month-grid `/calendar` (violet events, ☐ open task due dates, prev/today/next). Migration `012_tasks_calendar.sql` (also adds "org members read co-members" profiles policy for assignee dropdowns, and turns modules on for all existing workspaces). Module visibility per workspace via `crm_config.modules` toggles on `/crm`; Clancy workspace always shows both.
- **Batch 7 shipped (2026-07-10): roles, invites, calendar categories, website signup automation.** Roles viewer/editor/admin per workspace (labels renameable in `crm_config.role_labels`; keys fixed; 'owner' mapped to admin) — enforced in `lib/permissions.ts` at the server-action/page layer (org boundary stays RLS; intra-org role bypass via direct PostgREST is a known accepted gap, RLS hardening is future work). Departments (`crm_config.departments`) scope task visibility for non-admins (own dept + shared). Team page is now workspace-scoped: members (role+dept), email invites (`org_invites`, consumed by `handle_new_user`), renameable role labels, departments + calendar category editors; platform-admin account list preserved at bottom. Calendar events carry a category → coloured chips + legend. Client sites can enable a **signup form** section (`form` in `lib/sections`, config `form_*`) → `submit_lead()` RPC creates a record in stage 1 + an Inbox notification (`notifications` table, header badge, `/notifications`). Workspace admins can now edit their own site (Website nav link) and Customize page. Migration `013_roles_invites_forms.sql`.
- **Batch 8 shipped (2026-07-10): rich calendar events, site favicon, CRM build brief.** Events now carry start/end date+time, all-day flag, recurrence (`repeat`: none/daily/weekly/biweekly/monthly/yearly, expanded at read time in `lib/recurrence.ts` — no occurrence rows), per-department alerts, and an alert-lead-time; the add-event form is a proper expandable panel (`components/calendar/EventForm.tsx`, Apple-Calendar-style). Recurring instances show a ↻ marker; deleting removes the series. Migration `014_calendar_events_favicon.sql`. **Favicon:** `sites.config.favicon_url` (no DDL), uploaded via the site editor (reuses `SiteLogoUpload` into `site-assets` under `<slug>-favicon`), rendered as `<link rel=icon>` on `/s/<slug>`. **CRM build brief:** `buildCrmBrief()` in `lib/intake.ts` + `/clients/[id]/crm-brief` tab (website brief relabeled "Website brief") — compiles record type, pipeline stages (from intake `workflow.pipeline_steps`), fields, departments, follow-ups, and modules into a paste-to-Claude brief for configuring a client's back-end CRM. Two briefs, one intake.
- **Batch 9 / audit pass (shipped 2026-07-10):** (1) client website access from the workspace nav — "View site" for every member, "Edit website" for workspace admins; site-editor back-link now points non-platform admins to their live site instead of the platform Sites page. (2) **Timezone fix** — all "today"/current-month computations now use Malaysia time via `lib/dates.ts` (Vercel runs UTC; dates flipped after 8am/4pm MYT — this was the "calendar not synced" report). (3) Site **text colour override** (`config.text_color`, optional — auto-derives muted/faint via alpha). (4) **Broadcasts** (`/broadcasts`, editors+): announcements/newsletters to records with emails; audience all-or-stage; v1 delivery = pre-filled BCC mailto batches of 40 + copy-all + mark-as-sent (`broadcasts` table, migration `015_broadcasts.sql`); becomes real sending when the phase-2 email API lands. (5) Clancy HQ app favicon (`app/icon.svg` — the c. mark; client sites override via their own favicon). (6) Mobile: nav scrolls horizontally instead of wrapping/overflowing.
- **Batch 10 shipped (2026-07-10):** (1) **Automated email** via Gmail SMTP (`lib/email.ts`, nodemailer; env `GMAIL_USER` + `GMAIL_APP_PASSWORD` — falls back to mailto flows when unset; swap to Resend when clancy.my exists). (2) **Broadcasts v2** — Apple-Mail-style compose (To/Subject/Message rows); audiences now include team (whole/role/department) alongside records (all/stage) via `lib/audience.ts`; one-click "Send now" (BCC chunks of 40) when email configured. (3) **Editable landing page** — `/` renders from sites row `clancy-home` (seeded in migration `016_landing_config.sql`, published=true for anon read but blocked from `/s/`); pinned "Clancy homepage" card atop Sites → `/sites/home/edit`. (4) **Custom site sections** — `config.custom_sections` (heading+body, keys `custom-<slug>`), editable + reorderable via extended `SectionOrderEditor`/`resolveSectionOrder`. (5) **People directory** (`/people`, all members) — everyone in the workspace: records (website vs manual chips) + team, filter chips + search. (6) **Nav icons + active states** (lucide-react, `NavLink`). (7) **Invite emails** — sent automatically on invite when email configured; template per workspace (`crm_config.invite_subject/message`, tokens {business}/{link}) editable on Team; "Copy invite" fallback per pending invite. (8) **Bug fixes:** category/department key collisions (keys were minted from the first keystroke — caused wrong calendar colours vs legend; now slugged from full names + deduped; existing bad keys need re-creating the categories + re-adding affected events).
- **Batch 11 shipped (2026-07-31): broadcast attachments + HTML email + sign-off.** Broadcasts now send **HTML email** with a plain-text alternative, not bare text. `lib/broadcast-email.ts` renders a table-based, inline-styled, 600px light template — deliberately not Tailwind/dark-theme, because mail clients strip `<style>`, ignore flex/grid, and render dark backgrounds badly. Bare URLs auto-link (trailing punctuation and quote entities pushed outside the anchor). (1) **Attachments** — migration `017_broadcast_attachments.sql` adds `broadcasts.attachments jsonb` + a **private** `broadcast-files` bucket (org-scoped paths, policy mirrors `intake-files`); public `site-assets` was the wrong home for a client's price list. `components/AttachmentUpload.tsx` uploads under a client-generated draft id before the row exists (paths ride in a hidden input as JSON, same shape as the site editor), with drag-drop, thumbnails from local object URLs, and an **in-message / attached** toggle per image. Images default to inline. Server downloads each file once under the caller's session and hands nodemailer a buffer; inline images embed via `cid:`, so nothing is ever world-readable. 18MB cap (Gmail's real limit is 25MB; base64 adds ~33%). (2) **Sign-off** — `crm_config.signature` (no DDL, so it's per-workspace automatically): logo, business name, tagline, sign-off line, sender name/title, phone/email/website/address, small print. Edited on **Team** under Workspace settings with a preview of the saved block; Broadcasts shows whether one is set and links across. The logo is fetched and embedded via cid too, so it survives clients that block remote images (fetch restricted to http(s)). Gmail SMTP always sends *from* the authenticated account, so `fromName` only controls the display name — a broadcast reads as the client's business, not "Clancy". (3) **Review page** — the raw `<pre>` became a sandboxed iframe of the real rendered email, plus a file list. (4) **Fallback mode is honest:** `mailto:` has no attachment parameter, so with no Gmail password configured the files become 7-day signed download links in the body and the page says so plainly. (5) **Bug fix:** `createBroadcast` counted recipients off `clients` regardless of audience, so every *team* broadcast showed 0 recipients until sent; it now uses `resolveAudience`.
- **Batch 12 shipped (2026-09-02): responsive pass — mobile + desktop.** Every app page now works at 360px and unchanged at ≥1024px. (1) **Header nav** — `components/MobileNav.tsx` (new): hamburger + left slide-over drawer below `lg`, with the workspace switcher and Sign out in the drawer footer; the horizontal icon nav is `hidden lg:flex`. Nav links are now derived once as a `NavItem[]` in `Header.tsx` and rendered by both surfaces, so there is one list, not two. Header is `sticky top-0 z-40` with a **solid** `bg-graphite` — `backdrop-blur` was tried and reverted because `backdrop-filter` makes the header a containing block for `position: fixed` descendants, which clipped the drawer to the header's height. Inbox collapses to an icon + badge under `sm`. (2) **Global control sizing** — `app/globals.css` forces 16px on `input`/`select`/`textarea` under 640px (iOS Safari zooms the viewport on focus below 16px) plus `overflow-wrap: break-word` on body. (3) **Conventions applied everywhere** — page containers `mx-auto w-full max-w-* px-4 py-8 sm:px-6`; every form `grid-cols-2/3` became `grid gap-3 sm:grid-cols-*` (single column on a phone); action rows `flex-wrap` or `flex-col sm:flex-row`; touch targets `py-2 sm:py-1.5`; long emails/slugs/URLs `min-w-0` + `truncate`/`break-words`. (4) **Kanban boards** (`/pipeline`, `RecordsBoard`) — columns `w-[78vw] max-w-[17rem] sm:w-64` inside an edge-to-edge `-mx-4 … snap-x snap-mandatory` scroller, so one column reads full-width with the next peeking. (5) **Calendar** — month grid still 7 columns at 360px: single-letter weekday headers under `sm`, `min-h-16 sm:min-h-24` cells, truncated chips; date+time pairs in `EventForm` stack. (6) **List editors** (services/FAQ/steps/socials/custom sections/CRM fields/departments/categories) — stacked cards on mobile, the existing single-row grid on desktop. 50 files touched, no behaviour, copy, colour, or config-key changes; `tsc --noEmit` clean.
- **Batch 13 shipped (2026-09-06): Clancy-staff access toggle + free-typed broadcast recipients.** (1) **Clancy staff toggle** — `profiles.is_platform_admin` has existed since migration 004 with RLS policies, but there was never a UI, so only the bootstrapped accounts had it and new staff needed SQL. Team → *All accounts* now carries a "Clancy staff" checkbox per account (platform admins only, guarded by `requireAdmin()`). Three lockout guards, since admin lockout has happened twice: you cannot untick yourself (checkbox disabled, value resent via a hidden input so a disabled control doesn't read as "unchecked"), the **last** platform admin cannot be demoted (server-side count check), and the section carries explicit warning copy that the flag reaches every client's data. **Deliberately NOT done:** giving workspace admins cross-workspace powers — that would break the RLS wall between client businesses. Client-side admins stay confined to their own workspace; the two admin concepts are distinct and stay distinct. No migration. (2) **Free-typed broadcast recipients** — migration `018_broadcast_custom_recipients.sql` adds `broadcasts.custom_recipients jsonb` (its own column, not stuffed into the `audience` text field, which would wreck the audience label). Compose gains an "Also to" row; the audience picker gains "Just the addresses I type below" (`audience = 'custom'`, which `resolveAudience` short-circuits to `[]`). `parseEmailList()` in `lib/audience.ts` accepts commas/semicolons/spaces/newlines and `Name <addr>` pairs — it splits on record separators *first*, then handles angle-bracket entries, and strips quoted display names before splitting, because Gmail's `Jacky Wong <a@b.com>` reported the display name as an invalid address and Outlook's `"Wong, Jacky" <a@b.com>` split on the comma inside the quotes; both would have rejected the whole send. `mergeRecipients()` dedupes case-insensitively with audience recipients winning (so a person who is both a record and a typed address keeps their real name and is emailed once). Typed addresses show a `typed` chip on the review page. (3) **Bug fix:** `createBroadcast` counted recipients off `clients` regardless of audience, so every *team* broadcast displayed 0 recipients until it was actually sent; it now uses `resolveAudience` + `mergeRecipients`.
- **Batch 14 shipped (2026-09-06): stage checklists — the SOP layer.** The first piece of the **operations-ERP pivot** (researched 2026-09-06): tasks that appear automatically when a record enters a stage, turning a pipeline into an enforced workflow. Migration `019_stage_checklists.sql` adds `pipeline_stages.checklist jsonb` (`[{title, details?, department?, due_in_days?, blocking?}]`) plus `tasks.origin_stage_id` and an index on `(client_id, origin_stage_id)`. The template lives on the **stage**, not in `crm_config`, so a future workflow pack snapshots stages *and* their process as one object. `lib/checklist.ts` holds the logic: `generateStageTasks()` is **idempotent** — it checks `origin_stage_id` before inserting, so moving a record out of a stage and back never duplicates work; `blockingTasksFor()` returns unfinished `blocking` items. `moveClientStage()` is the single choke point for both boards, so one hook covers client workspaces *and* Jacky's own Clancy sales pipeline. **Blocking only stops FORWARD moves** (compared by `position`) — sending a job back to an earlier stage must stay possible even with work outstanding. Record creation generates the stage-1 checklist too. **No backfill by design** (Jacky's call): existing records get nothing until they move, with a "Generate checklist" button on the record for pulling them in one at a time — auto-backfilling would mass-create tasks across every record at once. UI: collapsible checklist editor per stage on `/stages` (admins only, `ChecklistEditor` follows the existing list-editor pattern); a tick-off checklist block on `/records/[id]` with a `blocks` chip on outstanding blockers; an `n/m` progress pill on board cards counting **only** the record's current stage. Also fixed in passing: `PipelinePage` accepted no `searchParams` and `RecordsBoard` rendered no errors, so a blocked move would have failed **silently** — both now surface the reason (naming the outstanding tasks). `addDays()` added to `lib/dates.ts` (all date math goes through that file; parsed as UTC midnight so server timezone can't shift it).
- **Batch 15 shipped (2026-09-06): checklist visibility fix + `/stages` → `/workflow`.** Two parts, both from Jacky's smoke test failing. (1) **The fix.** Batch 14's checklist UI shipped only on `/records/[id]`, which Clancy's own workspace *redirects away from*, and the progress pill only on `RecordsBoard`, which Clancy's workspace doesn't render — so on Jacky's own workspace the feature had **no interface at all** and "Generate checklist" was unfindable. New shared `components/StageChecklist.tsx` serves both `/records/[id]` and `/clients/[id]`; `stageProgressByRecord()` in `lib/checklist.ts` drives the pill on **both** boards; the blocked-move notice renders on the Clancy board too. `generateStageTasks` is now idempotent **by title** rather than all-or-nothing — the old check made "Add this stage's tasks" silently do nothing whenever the record already had one task from that stage, so items added to a stage after a record arrived could never be pulled in. **Errors are returned and surfaced, never swallowed as `0`** — a silent return is precisely what made the original bug undiagnosable from outside; likewise `saveStageChecklist` selected the updated row back so a write matching zero rows stopped reporting "Saved." (2) **`/workflow` replaces `/stages`.** The old page put each stage's checklist behind a disclosure triangle beside a *rival* Save button, so checklist edits were easy to lose and the workflow was never visible end to end. `/workflow` shows stages as columns with their checklists inline, `←/→` reordering (positions derived from order on save), a **"Copy from…"** picker to clone a checklist between stages, a per-column plain-English summary of what a record entering that stage gets, and **one** `saveWorkflow` action covering names, order and every checklist. `/stages` remains as a redirect so old links land. `ChecklistEditor` and `saveStageChecklist` removed as superseded.
- **Roadmap:** rules engine (trigger → action) then workflow packs — the two remaining ERP layers; calendar day/week views; per-event alert delivery (cron/scheduled sends); booking engine (phase 2); domain + Resend when clancy.my registered. A **second build brief (CRM brief)** — analogue of the website brief — is planned to configure a new client's crm_config + stages from intake.

## Dev workflow (repo rules — mirrors megastar-crm)
- **`PROGRESS.md` is the session handoff** — current state, what's blocked on Jacky (unrun migrations, email setup, data repairs), open decisions, risks, and next build candidates. Read it at the start of a session; update it at the end.
- **`OPERATIONS.md` is the runbook** — how the whole business runs stage-by-stage (lifecycle, Claude handoff, admin how-tos, infra map, hard lessons). Read it when recalling how Clancy operates; update it when the operating flow changes.
- **Status 2026-07-07: fully operational end-to-end** — first complete loop verified live (client → intake with file upload → build brief → Claude downloaded the files from the brief's signed links).
- **Batch 4 (shipped 2026-07-08):** client-facing intake links (`/i/<token>` — anon access via security-definer RPCs only, migration `008_client_intake_link.sql`; client answers MERGE into intake data; "Copy client link" on the intake tab) · Sites admin (`/sites` list/create + `/sites/<slug>/edit` full config editor with logo upload to `site-assets`, publish toggle) · workspace switcher in the header (admins; switches own profile's org). Brand contact = **clancy.hq.ai@gmail.com**, IG **@clancy.hq** (see memory: reference-clancy-accounts; passwords not stored). Landing CTA now mails the brand address; IG link in footer.
- **Website engine v0.1 (build plan phase 3 seed, shipped 2026-07-07):** public multi-tenant sites at `/s/<slug>`, rendered from `sites.config` JSON (migration `007_sites.sql`; anon-readable when `published`, plus public `site-assets` bucket). Booking CTA = WhatsApp deep link until phase 2 booking exists. First tenant site: By You Enterprise rehearsal (`/s/byyou`, org #2, F&B stage set). No editor UI yet — config changes via SQL/Claude; site editor is a batch-4+ candidate.
- **`config.socials` added 2026-07-09:** optional `{ platform, url }[]` on `SiteConfig` — renders as links in the public site footer, edited via `SocialsListEditor` on the site editor. Generic/reusable (any client with social links), not bespoke.
- **`config.theme` ('light' | 'dark') added 2026-07-09:** per-tenant colour scheme on the public site — the page derives a palette (bg/surface/text/border/muted) from it; light is the default, accent colour is independent. Theme selector sits next to accent in the site editor. Reusable — first used to give SGCKL a black bg + orange (Claude-clay) accent.
- **Template-depth batch (2026-07-09):** `SiteConfig` gains `book_label`, `logo_position` ('left'|'center'), and editable section headings `services_title`/`find_us_title`/`faq_title` — all optional, fall back to defaults. Empty service `price` now renders nothing. Site editor exposes all of them. This was the **decided direction on the "make sites more customisable" question**: deepen configurability of the single curated template, explicitly NOT a Canva-style drag-and-drop builder (rejected — multi-year, competes with Wix/Canva, and Managed clients wouldn't use it; design freedom is delivered via Claude + curated variants). Next phase if pursued = a sections + layout-variants + per-vertical template-packs system (curated, not free-canvas).
- **WhatsApp messages + full-width + typography batch (2026-07-09):** (1) `book_message`/`service_message` — editable WhatsApp templates per contact button with `{name}`/`{service}` tokens. (2) Site widened `max-w-3xl`→`max-w-7xl` with responsive padding + denser desktop grids (3-col services, 4-col gallery, 2-col FAQ) — fixed the "looks like mobile / deadspace" complaint. (3) **Per-text-type typography** `type_headings`/`type_body`/`type_buttons` (`TypographyStyle`: font override [curated key or inherit] + bold + italic + align) — the decided granularity was **by text type, site-wide**, NOT per-element/page-builder. Applied to all headings, key body paragraphs, CTA buttons (button align drives hero CTA); all fonts load in one combined Google Fonts request. **Client self-editing was declined for now** — sites stay Jacky-edited (Managed model); client logins deferred. **Still open (the parked "sections engine"): reorderable sections synced to the live page** — Jacky wants it; recommended as its own later phase after typography.
- **Perf pass (2026-07-09):** app had zero `loading.tsx` and plain submit buttons → every click waited on the server round-trip with no feedback. Added `PageSkeleton` + `loading.tsx` for pipeline/clients/sites/stages/team (instant nav feedback, also lets Next prefetch the loading boundary; public `/s` `/i` untouched) and `SubmitButton` (`useFormStatus`, disables + pending text) on the main save/create forms. NOT touched (trust-boundary caution): the middleware/Header `getUser()` auth round-trips — a safe follow-up is deduping them via React `cache()`.
- **Contact method (WhatsApp | Email) SHIPPED 2026-07-09:** `config.contact_method` ('whatsapp' default | 'email') + `contact_email` + `email_subject`/`email_message` (drafted templates, {name}/{service} tokens). One `contactHref(config,name,service)` helper drives the booking + service buttons for whichever method is chosen — WhatsApp → wa.me (unchanged), email → `mailto:` with encoded subject/body. Find us section also shows the contact email as a mailto when set. Editor: "Contact method" selector + email fields beside the WhatsApp drafts. Backward-compatible (absent → WhatsApp). Decided as an **either/or per-site choice** (not both buttons shown); showing both is an easy extension if wanted. Verified: SGCKL stays WhatsApp, new sites default safely, mailto construction correct.
- **Sections engine — Phase 1 SHIPPED 2026-07-09:** ordering layer (not a content rebuild). `config.section_order?: string[]` = ordered enabled keys among about/services/gallery/find_us/faq (`lib/sections.ts`: `SECTION_KEYS`, `DEFAULT_SECTION_ORDER`, `resolveSectionOrder`). The public page maps over the resolved order (hero pinned top, footer bottom); absent/empty → default order, so pre-existing sites are unchanged (verified on `/s/sgckl`). Content still lives in the flat config fields. `SectionOrderEditor` (▲▼ + Show toggle, no dnd dependency) sits in a "Page sections & order" group near the top of the site editor. **Deferred:** Phase 2 = drag-and-drop polish + hero movable + per-section bg/spacing; Phase 3 (only if truly needed) = full block model (`sections: {id,type,...}[]`, multiple instances/custom blocks) — the page-builder direction we deliberately avoid. `SiteConfig` adds: `bg_color` (hex override, auto-contrast text via luminance), `bg_image_url` (fixed bg + dark overlay), `font`/`font_custom` (curated list in `lib/fonts.ts` + any Google Font, loaded via a per-site `<link>`), `hero_image_url` (photo behind headline), `gallery`+`gallery_title` (photo grid section), `about_title`/`about_body`/`about_image_url`/`about_image_side` (image+text block), `service_book_label`. New reusable components: `ColorField` (picker + type-a-hex, synced), `ImageUpload` (generalised from `SiteLogoUpload`), `GalleryUpload` (multi-image). Site editor regrouped into labelled sections (Basics / Contact / Appearance / Hero / Buttons & headings / About / Services / Gallery / FAQ / Socials). All optional; images upload to the `site-assets` bucket at `orgId/slug/...`. Verified live on `/s/sgckl`.
- **Second tenant site: SGCKL** (church, KL — migration `009_sgckl.sql`, org `sgckl`, `/s/sgckl`): visitor-retention pipeline (New Visitor → Contacted → Attending → In a CG → Serving → Member), site seeded from the SGCKL build brief with FB/IG/YouTube socials, red accent `#D1362F`, tagline "Creative. Believe. Live." (from logo). Logo image still needs uploading via the site editor. Note: this client came in as a **test** (brief flags placeholder SSM/phone), built for real to exercise the church vertical + socials feature. No payment integration (DuitNow/bank transfer out of scope — no gateway yet); domain left on `/s/sgckl` (brief said "let Claude decide").
- **This folder is the Clancy app repo** (Next.js 16 + Supabase + Vercel at clancy-hq.vercel.app, same stack as `../megastar-crm`, zero shared infrastructure with it). Batch 1 shipped 2026-07-07: auth, multi-tenant schema (`supabase/schema.sql`), pipeline board, add-client form; Clancy = organization #1. Batch 2 shipped same day: client detail/edit page, 9-section intake module (definition as data in `lib/intake.ts`, answers as JSON in `intakes` — migration `003_intake.sql`), build-brief generator with copy button, intake progress on pipeline cards. Batch 3 shipped same day: public landing page at `/` (logged-out visitors see it; CTA is a mailto placeholder — replace with business WhatsApp), stage management page (`/stages`: rename/reorder/add/delete with in-use guard), client delete with confirm. **UI theme is dark since 2026-07-07 (Jacky's call): graphite `#1E1D21` background, ivory `#F4EFE6` text, carbon `#26252B` surfaces, ash `#3A3940` borders, violet accents unchanged** — supersedes the porcelain-light surfaces in branding-marketing.md for the app UI. Google OAuth button is env-gated behind `NEXT_PUBLIC_ENABLE_GOOGLE_AUTH=true` — flip it only after the Google provider is configured in Supabase + Google Cloud.
- Claude has no DB access — paste all SQL inline in chat; Jacky runs it in the Supabase SQL Editor. Keep `supabase/schema.sql` updated as migrations accrue.
- Draft-first before any nontrivial batch. `npm run typecheck` before every push. Push = production deploy (Vercel auto-deploys from main) and stays permission-gated.
- Auth model (since migrations 002+004): signup is open (email/password + Google OAuth); new users get an **org-less profile** — RLS blocks all data and they land on `/no-access` until access is granted on the **/team page** (platform admins only; Jacky's accounts were bootstrapped as admins in 004). No SQL needed for grants anymore. Google button is env-gated (`NEXT_PUBLIC_ENABLE_GOOGLE_AUTH=true`, on in prod + `.env.local` since 2026-07-07).

## Open questions
- Confirm entity registration (then update governance flags + contracts)
- Verify + register domain/handles (clancy.my preferred — see branding-marketing.md)
- Define the full-time revenue gate (proposed in the 5-year plan — needs Jacky's number)
- Commit to checkpoints? (guards against open-ended drift with no deadline)
- Pilot client sourcing for the workshop vertical

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `jackywong29/clancy`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, unchanged: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
